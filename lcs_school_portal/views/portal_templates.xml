<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <template id="school_login_template" name="School Portal Login">
        <!DOCTYPE html>
        <html>
            <head>
                <meta charset="utf-8"/>
                <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
                <title>School Portal Login</title>
                <link rel="stylesheet" type="text/css"
                      href="/web/static/lib/bootstrap/dist/css/bootstrap.min.css"/>
                <style>
                    body { background: #f4f5f7; }
                    .login-card { max-width: 420px; margin: 8vh auto; }
                    .login-card .card-body { padding: 2rem; }
                    .login-card h2 { font-weight: 600; margin-bottom: 1.5rem; text-align: center; }
                </style>
            </head>
            <body>
                <div class="container">
                    <div class="card shadow-sm login-card">
                        <div class="card-body">
                            <h2>School Portal</h2>
                            <form role="form" t-attf-action="/school/login" method="post"
                                  onsubmit="this.action = this.action + location.search">
                                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                <input type="hidden" name="redirect" t-att-value="redirect"/>

                                <div class="mb-3">
                                    <label for="class_label" class="form-label">Class</label>
                                    <select name="class_label" id="class_label" class="form-select" required="required">
                                        <option value="">— Select Class —</option>
                                        <t t-foreach="classes" t-as="cls">
                                            <t t-set="lbl" t-value="cls.name.split('-')[-1].strip() if '-' in cls.name else cls.name"/>
                                            <option t-att-value="lbl" t-att-selected="lbl == class_label">
                                                <t t-esc="cls.name"/>
                                            </option>
                                        </t>
                                    </select>
                                </div>

                                <div class="mb-3">
                                    <label for="student_name" class="form-label">Student Name</label>
                                    <input type="text" name="student_name" id="student_name"
                                           class="form-control" t-att-value="student_name"
                                           required="required" autocomplete="off"
                                           autofocus="autofocus" placeholder="e.g. 陳大文"/>
                                </div>

                                <div class="mb-3">
                                    <label for="password" class="form-label">Parent Phone (Password)</label>
                                    <input type="password" name="password" id="password"
                                           class="form-control" required="required"
                                           autocomplete="current-password"/>
                                </div>

                                <div class="alert alert-danger" role="alert" t-if="error">
                                    <t t-esc="error"/>
                                </div>

                                <div class="d-grid gap-2 mt-3">
                                    <button type="submit" class="btn btn-primary">Log in</button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </body>
        </html>
    </template>

</odoo>
